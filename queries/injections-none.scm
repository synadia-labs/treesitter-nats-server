; Empty injection file — disables all template language injection.
;
; Template delimiters are still recognized and highlighted via
; highlights.scm, but no inner language grammar is injected.
;
; Use this if you want template delimiter highlighting without
; language-specific syntax highlighting inside the delimiters.
